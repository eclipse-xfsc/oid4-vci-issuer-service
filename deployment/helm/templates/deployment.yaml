apiVersion: apps/v1
kind: Deployment

metadata:
  name: {{ .Chart.Name | quote }}
  namespace: {{ .Release.Namespace | quote }}
  labels:
    app: {{ .Chart.Name | quote }}

spec:
  replicas: {{ .Values.replicaCount }}

  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0

  selector:
    matchLabels:
      app: {{ .Chart.Name | quote }}

  template:
    metadata:
      labels:
        app: {{ .Chart.Name | quote }}
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}

    spec:
      serviceAccountName: {{ .Values.serviceAccountName | quote }}

      {{- with .Values.image.pullSecrets }}
      imagePullSecrets:
        - name: {{ . | quote }}
      {{- end }}

      containers:
        - name: {{ .Chart.Name | quote }}
          image: "{{ .Values.image.repository }}/{{ .Values.image.name }}:{{ default .Chart.AppVersion .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy | quote }}

          env:
            {{- $podAnnotations := default dict .Values.podAnnotations }}
            {{- if eq (default "" (index $podAnnotations "inject.xfsc.io/enabled")) "false" }}
              # injected
            - name: ISSUANCE_NATS_URL
              value: {{ .Values.config.nats.url }}
            # x-jwks-url if not set
            - name: ISSUANCE_JWKSURL
              value: {{ .Values.config.jwksUrl}}
            # injected
            - name: ISSUANCE_DID_RESOLVER
              value: {{ .Values.config.didResolver }}
            # x-audience-url if not set
            - name: ISSUANCE_AUDIENCE
              value: {{ .Values.config.audience }}
            {{- end }}

            - name: ISSUANCE_LISTEN_ADDR
              value: {{ .Values.server.http.host | quote }}

            - name: ISSUANCE_LISTEN_PORT
              value: {{ .Values.server.http.port | quote }}

            - name: ISSUANCE_NONCE_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ .Chart.Name }}-nonce-secret
                  key: nonce-secret

          {{- with .Values.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}

          ports:
            - name: http
              containerPort: {{ .Values.config.servingPort }}
              protocol: TCP

          volumeMounts:
            - name: config-volume
              mountPath: /app

      volumes:
        - name: config-volume
          configMap:
            name: "{{ .Release.Name }}-configmap"
